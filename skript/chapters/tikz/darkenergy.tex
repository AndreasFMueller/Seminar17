%
% dargekenergy.tex -- Entwicklung des Skalenfaktors in einem realistischen
%                     Universum
%
% (c) 2017 Prof Dr Andreas Müller, Hochschule Rapperswil
%
\documentclass[tikz]{standalone}
\usepackage{times}
\usepackage{txfonts}
\usepackage{pgfplots}
\usepackage{csvsimple}
\usetikzlibrary{arrows,intersections}
\begin{document}
\begin{tikzpicture}[thick,scale=2.5]
\coordinate (O) at (0,0);
\begin{scope}
\clip (-2.5,0) rectangle (3,3);
\csvreader[head to column names,%
	late after head=\xdef\told{\t}\xdef\old{\a},%
	after line=\xdef\told{\t}\xdef\old{\a}]%
	{darkenergy.csv}{}{%
	{\ifthenelse{\lengthtest{\a pt > 0.001pt}}{%
		\draw[pink,line width=1.5] (2 * \told,\old)--(2 * \t,\a) node {};
	}{}};
}

\csvreader[head to column names,%
	late after head=\xdef\told{\t}\xdef\old{\b},%
	after line=\xdef\told{\t}\xdef\old{\b}]%
	{darkenergy.csv}{}{%
	{\ifthenelse{\lengthtest{\b pt > 0.001pt}}{%
		\draw[red,line width=1.5] (2 * \told,\old)--(2 * \t,\b) node {};
	}{}};
}

\csvreader[head to column names,%
	late after head=\xdef\told{\t}\xdef\old{\c},%
	after line=\xdef\told{\t}\xdef\old{\c}]%
	{darkenergy.csv}{}{%
	{\ifthenelse{\lengthtest{\c pt > 0.001pt}}{%
		\draw[red,line width=1.5] (2 * \told,\old)--(2 * \t,\c) node {};
	}{}};
}

\csvreader[head to column names,%
	late after head=\xdef\told{\t}\xdef\old{\d},%
	after line=\xdef\told{\t}\xdef\old{\d}]%
	{darkenergy.csv}{}{%
	{\ifthenelse{\lengthtest{\d pt > 0.001pt}}{%
		\draw[red,line width=1.5] (2 * \told,\old)--(2 * \t,\d) node {};
	}{}};
}

\csvreader[head to column names,%
	late after head=\xdef\told{\t}\xdef\old{\e},%
	after line=\xdef\told{\t}\xdef\old{\e}]%
	{darkenergy.csv}{}{%
	{\ifthenelse{\lengthtest{\e pt > 0.001pt}}{%
		\draw[red,line width=1.5] (2 * \told,\old)--(2 * \t,\e) node {};
	}{}};
}

\csvreader[head to column names,%
	late after head=\xdef\told{\t}\xdef\old{\f},%
	after line=\xdef\told{\t}\xdef\old{\f}]%
	{darkenergy.csv}{}{%
	{\ifthenelse{\lengthtest{\f pt > 0.001pt}}{%
		\draw[red,line width=1.5] (2 * \told,\old)--(2 * \t,\f) node {};
	}{}};
}

\csvreader[head to column names,%
	late after head=\xdef\told{\t}\xdef\old{\g},%
	after line=\xdef\told{\t}\xdef\old{\g}]%
	{darkenergy.csv}{}{%
	{\ifthenelse{\lengthtest{\g pt > 0.001pt}}{%
		\draw[red,line width=1.5] (2 * \told,\old)--(2 * \t,\g) node {};
	}{}};
}
\end{scope}

\draw[blue,line width=1.5] (-2,0)--(3,2.5);

\draw[->] (-2.55, 0   )--(3.05,  0   ) coordinate[label = {below:$t$}];
\draw[->] ( 0,   -0.05)--(0   ,  3.05) coordinate[label = {right:$a(t)$}];

\node at ( 0,0) [below right] {$t_0$};
\node at (-2,0) [below] {$\displaystyle -\frac1{H_0}$};
\node at (-1.333,0) [below] {$\displaystyle -\frac2{3H_0}$};

\draw (-2,-0.03)--(-2,0.03);
\draw (-1.3333,-0.03)--(-1.3333,0.03);

\node at (3.05,2.1) [below left] {$\Lambda=0$};
\node at (2.10,2.85) [above left] {$\Lambda=1$};

\end{tikzpicture}
\end{document}


